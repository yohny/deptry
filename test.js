var assert = require('assert');

describe('add', function() {
  it('should be correct sum 1', function() {
    assert.equal(3, 2 + 1);
  });

  it('should be correct sum 2', function() {
    assert.equal(5, 2 + 3);
  });
});
